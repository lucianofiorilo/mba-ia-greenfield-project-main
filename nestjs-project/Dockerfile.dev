FROM node:25.6.0-slim

# ffmpeg/ffprobe are needed so the video-processing integration tests (which run
# in this container) can exercise the real processor, matching the worker image.
RUN apt update && apt install -y procps curl ffmpeg

WORKDIR /home/node/app

USER node

CMD [ "tail", "-f", "/dev/null" ] 